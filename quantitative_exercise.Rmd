---
title: "Quantitative Exercise"
author: "Marine Collery and Benjamin Seregi"
date: "10/29/2017"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Introduction

We are looking at the Monty Hall problem.

## Dataset

The dataset is generated by a C program that simulates the multiple cases.

Setting the directory to the data directory.
```{r}
getwd()

setwd("/Users/Marine/Documents/1_KTH/4_Courses/3_RMSW/Assignments/Quantitative")

getwd()
```


Read in data frame

```{r}

mdf <- read.csv("data.csv",header=T,sep=',',stringsAsFactors = F)

# check what variables are there
names(mdf)
summary(mdf)
```

```{r}

#factorize the data

#mdf$doorf=as.factor(mdf$door)
#names(mdf)[3]='label'
mdf$Sf=as.factor(mdf$S)
mdf$NSf=as.factor(mdf$NS)
summary(mdf)
```

```{r}
#Count all the wins in 2 sets : Switching or Not
countWin <- function(df) {
    # df is a dataframe - want to get first character of "phases" label and insert into label2 column
    
    l1 <- length(df$NS)
    sumWinS <- 0
    sumWinNS <- 0
    
    for (i in 1:l1) {
       
        if (df$S=='w' & df$NS=='f') {
          sumWinS <- sumWinS+1
          df$sumWinSwitched[i] <- sumWinS
          
          #Not changed:
          df$sumWinNotSwitched[i] <- sumWinNS
        }
        else if (df$S=='f' & df$NS=='w'){
          sumWinNS <- sumWinNS+1
          df$sumWinNotSwitched[i] <- sumWinNS
          
          #Not changed:
          df$sumWinSwitched[i] <- sumWinS
        }
    }
    return(df)
    
}

mdf <- countWin(mdf)

summary(mdf)
```

### Variables

#### Independant Variables


#### Dependant Variables


## Analysis


```{r}
plot(mdf$sumWinNotSwitched, xlim=c(0.0,length(mdf$NS)), ylim=c(0.0,length(mdf$NS)), xlab='Try', ylab='Number of Win', col='blue', type="b", lty=1)


#par(new=T)
#plot(mdf$sumWinSwitched, xlim=c(0.0,length(mdf$NS)), ylim=c(0.0,length(mdf$NS)), xlab='', ylab='', col='red')

lines(mdf$sumWinSwitched, pch=18, col="red", type="b", lty=2)

legend(0, length(mdf$NS)-1, legend=c("No Switch", "Switch"),
       col=c("blue", "red"), lty=1:2, cex=0.8)

#par(new=F)




```









## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
